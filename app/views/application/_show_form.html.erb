<h1><%= button_to("Edit", edit_movie_path(f), method: :get) %></h1>
<% @locals.each do |f| %>
  <h1><%= link_to(f.name, movies_path(f), method: :get ) %></h1>

  <h1><%= button_to("New", new_movie_path, method: :get) %></h1>
  <h1><%= button_to("Upvote", movie_path, method: :get) %></h1>
<% end %>
