
<h1 class= "tops"> Top Movies Top Books Top Albums </h1>
<% i.each do |f| %>
<td><%=link_to("#{f.name}", item, method: :get ) %></td>
<td>Ranked:<%= f.upvote.count %></td>
<% end %>

<h1><%= link_to("View More #{i.media_type}", items, method: :get) %></h1>

<% item.each do |f| %>
<td><%=link_to("#{f.name}", item, method: :get ) %></td>
<td>Ranked:<%= f.upvote.count %></td>
<% end %>

<h1><%= link_to("View More }", items, method: :get) %></h1>

<% items.each do |f| %>
<td><%=link_to("#{f.name}", item, method: :get ) %></td>
<td>Ranked:<%= f.upvote.count %></td>
<% end %>

<h1><%= link_to("View More #{items.media_type}", items, method: :get) %></h1>
